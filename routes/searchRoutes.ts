import { Router } from 'express';

export const SearchRoutes = Router().get('/', async (req, res) => {
  // Perform search
  // TODO - finalize search strategy
  // TODO - determine if filtering based on uri query string params
});
